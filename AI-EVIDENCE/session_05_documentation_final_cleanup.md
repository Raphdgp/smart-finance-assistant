# Session 05 Evidence – Documentation, README, and Final Cleanup

## Artifact 1: README Update

**Development issue:**  
The original README file still looked like the starter template. It described what students should build rather than explaining the completed Budget Buddy project.

**Prompt summary:**  
I asked AI to check the README and tell me what needed to be changed so that it matched my actual project.

**AI response summary:**  
AI identified that the README needed to be rewritten around the completed Budget Buddy assistant. It suggested replacing the generic template text with project-specific sections explaining:
- the Budget Buddy project overview;
- the business problem;
- main features;
- repository layout;
- how to run the project;
- sample data;
- testing;
- AI collaboration;
- limitations.

**README change made:**  
The README title was changed from the generic project template title to:

```markdown
# 📊 Budget Buddy – Smart Finance Assistant
```

**Example updated README section:**

```markdown
## 📖 Project Overview

Budget Buddy is a Smart Finance Assistant designed for students and young adults who want to understand their spending habits from transaction data.

The assistant uses a CSV file containing transaction records and turns it into useful finance insights. It can identify the largest spending categories, calculate net expenses, handle refunds, estimate savings based on monthly income, answer finance questions through a chatbot-style response, and calculate how long it will take to reach a savings goal.
```

**Result:**  
The README became specific to the completed project instead of reading like the original scaffold.

---

## Artifact 2: Correcting Outdated README Information

**Development issue:**  
After the chatbot was updated to use the `hands-on-ai` external server, part of the README was no longer accurate. It still said that the chatbot used rule-based finance logic.

**Prompt summary:**  
I asked AI to check the updated README and notebook for anything that was inaccurate or needed updating.

**AI response summary:**  
AI identified that the README limitation section still said:

```markdown
- The chatbot uses rule-based finance logic and CSV context rather than making uncontrolled financial claims.
```

This was outdated because the chatbot had been changed to use `hands-on-ai`.

**Correction made:**  
The limitation was updated to reflect the final project:

```markdown
- The chatbot uses the `hands-on-ai` external chat server with CSV-based transaction context.
- The chatbot response may vary slightly because it is generated by an AI model, but it is grounded using the transaction summary and retrieved CSV rows.
```

**Result:**  
The README now better matches the actual implementation.

---

## Artifact 3: Six-Step Methodology Cleanup

**Development issue:**  
I was unsure how to integrate the six-step methodology into the notebook. I did not know whether it should be repeated for every function or used once for the whole project.

**Prompt summary:**  
I asked AI whether the six-step methodology should apply to the whole project or section by section.

**AI response summary:**  
AI explained that the six-step methodology should be used as one overall development process for the project:
- Step 1: Understand the problem
- Step 2: Identify inputs and outputs
- Step 3: Work the problem by hand
- Step 4: Write pseudocode
- Step 5: Convert to Python
- Step 6: Test with different cases

The actual implementation cells then serve as Step 5, and the testing section serves as Step 6.

**Notebook update made:**  
The methodology section was rewritten to describe Budget Buddy specifically. It included:
- the finance problem being solved;
- the project inputs and outputs;
- manual calculations for grocery spending, refund handling, and savings rate;
- pseudocode for the full workflow;
- a note that the following Python cells implement the solution.

**Example manual calculation added:**

```text
Grocery spending = 45.50 + 120.00 + 156.00 + 73.20 + 145.60
Grocery spending = 540.30
```

**Result:**  
The methodology section became more connected to the actual project rather than remaining a generic template.

---


## Artifact 4: Updating RAG and Checklist Wording

**Development issue:**  
Some notebook headings and checklist items still referred to document retrieval, even though the actual project used CSV-based retrieval.

**Prompt summary:**  
I asked AI to check whether the notebook wording matched the final implementation.

**AI response summary:**  
AI advised changing the RAG heading from a generic document-based title to a CSV-specific one.

**Correction made:**  
The heading was changed to something like:

```markdown


## Reflection

This session focused on making the project readable, consistent, and ready for submission. The technical features were already working, but the documentation still needed to match the final implementation.

The main learning point was that code and documentation must stay aligned. For example, once the chatbot changed from a local rule-based response to a `hands-on-ai` response, the README and tests also needed to be updated. I also learned that starter-template cells can become misleading if they are not removed after the real project is built.

AI was useful for identifying inconsistencies that were easy to miss after working on the notebook for several hours. However, I still needed to decide which changes mattered most and avoid making unnecessary edits that could break a working notebook.